name: snackbar
description: Simple snackbar, requires the use of GetX
version: 1.0.2
homepage: https://github.com/ArcaneArts
environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
  get: ">=4.0.0 <=5.0.0"
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
